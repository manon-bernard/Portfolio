<div class="pill"><slot /></div>

<style>
  .pill {
    display: flex;
    padding: 0.5rem 1rem;
    gap: 0.5rem;
    color: var(--black);
    background-color: var(--accent);
    border-radius: 50px;
    font-size: var(--text-base);
    line-height: 1.35;
    white-space: nowrap;
    font-family: var(--ff-accent);
  }
</style>
